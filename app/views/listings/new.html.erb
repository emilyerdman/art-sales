<h1>Add a New Listing</h1>
<%= form_with model: @listing, local: true do |form| %>

  <%= render 'shared/errors', instance: @listing %>
  
  <%= render 'form', form: form %>

  <p>
      <%= form.submit %>
  </p>
  
<% end %>
